
First we wrote 'experiments/FrameTa.cpp' to compute the average time to read a video frame.

We can compile it (link with OpenCV installed with Brew) with:

```bash
g++ -o experiments/FrameTa experiments/FrameTa.cpp -I 
```